int main()
{
    // invalid name, because double is keyword in c++
    // int double=3.14;

    int _;
    // invalid name, beacause catch is keyword in c++
    // int catch-22;

    // invalid name, because it starts with a number
    // int 1_or_2 = 1;

    double Double = 3.14;
    return 0;
}
